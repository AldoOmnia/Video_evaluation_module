.DS_Store

# env / secrets
.env
.env.*
!.env.example

# build
dist/
build/
out/
*.tsbuildinfo

# deps
node_modules/

# python
__pycache__/
*.pyc
.venv/
venv/

# editor
.idea/
.vscode/
*.iml
*.log

# test
.coverage
.pytest_cache/

# OS
Thumbs.db

# Source asset dropoffs at the repo root — canonical copies live under
# eval-lab/public/assets/. Keeping the root files out of git prevents
# duplicate-asset confusion and a 600KB+ White.png blob in history.
/White.png
/Comer-industries_placeholder-logo.png

# Raw POV footage — large source videos stay out of git (regenerate the web
# transcode from source with ffmpeg). The small web-optimized POV clip that the
# synthetic-pov page streams (eval-lab/public/assets/phase8_pov.mp4, ~17 MB
# H.264 540x720) IS committed so it deploys to the hosted site.
/Phase_8_Video.MP4
*.MP4
# …but DO ship the small web POV clip (case-insensitive FS makes *.MP4 match it).
!eval-lab/public/assets/phase8_pov.mp4

# 3DGS reconstruction working files + raw 360 captures (large, local-only).
/reconstruction/
/360_World_Labs_Pinion_Guide/
/pinion_guide_station.JPG
eval-lab/public/assets/pgs_pano_*.jpg
eval-lab/public/assets/june18_pano_*.jpg
eval-lab/public/assets/pinion_guide_station.jpg
eval-lab/public/.station1_capture.json
eval-lab/public/.station_tour_calibration.json

# Facility knowledge-base store + uploaded reference images (runtime data,
# written by /api/kb — belongs to the deployment, not the repo).
eval-lab/public/.kb-store.json
eval-lab/public/kb-uploads/
eval-lab/public/assets/Industrial Machine Shop Interior 1.spz
eval-lab/public/assets/industrial-machine-shop-mesh.glb
eval-lab/public/assets/operator-omniverse.glb
eval-lab/public/assets/Worker/
June18_Panos/_previews/
