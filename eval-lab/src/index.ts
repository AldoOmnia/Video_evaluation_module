/**
 * eval-lab — public entry. The single-file brain-eval-lab.html v0.5
 * still lives at the repo root for the demo. As the lab grows past
 * that file we move pieces here and import them via <script type=module>.
 */
export * from "./api.js";
export * from "./sessionGen.js";
